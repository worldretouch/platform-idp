apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orders-api-staging
  namespace: argocd
spec:
  project: platform-services
  sources:
    - repoURL: https://github.com/myorg/platform-idp.git
      targetRevision: main
      path: shared/helm/platform-service
      helm:
        valueFiles:
          - $values/environments/staging/values/orders-api.values.yaml
    - repoURL: https://github.com/myorg/platform-env.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

